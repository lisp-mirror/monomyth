#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:prove) :silent t)
  )

(defpackage :ros.script.test-com-master.3802982138
  (:use :cl))
(in-package :ros.script.test-com-master.3802982138)

(defun main (&rest argv)
  (declare (ignorable argv))
  (prove:diag "Ready to start tests?")
  (read-line)
  (if (prove:run :monomyth/communication-tests-master) 0 1))
;;; vim: set ft=lisp lisp:
