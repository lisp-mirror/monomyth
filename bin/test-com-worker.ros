#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:rove) :silent t)
  )

(defpackage :ros.script.test-com-worker.3802982128
  (:use :cl))
(in-package :ros.script.test-com-worker.3802982128)

(defun main (&rest argv)
  (declare (ignorable argv))
  (if (rove:run :monomyth/communication-tests-worker) 0 1))
;;; vim: set ft=lisp lisp:
